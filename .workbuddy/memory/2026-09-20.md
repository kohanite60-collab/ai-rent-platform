# 2026-09-20

## 为简历输出项目文案

用户正在准备求职简历，要求基于本项目写一段项目经历文案（要求「一小段」）。
已交付一段：技术栈 + 项目简介 + 职责（20+ 接口、接口分层）+ 三项排查亮点
（MP 与 Boot 3 自动配置冲突、缺 `-parameters`、拦截器二次拦截 `/error` 掩盖异常）。

注意：**文案里的量化数字需用户自己核对，不要编造**；
用户尚未说明目标岗位（后端 / 全栈）与工作年限，若后续要调方向需先问。

## 前一日遗留的后端待办（用户尚未拍板）

1. 补 `GET /ai/task/list`（按当前登录用户查任务，解决 AI 创作页「我的任务」不可用）。
2. `/pay/notify` 放行 + 补 `AlipaySignature.rsaCheckV1` 验签（否则支付回调进不来、算力不到账）。
3. （可选）`server.error.include-stacktrace=never`，现在会把完整 Java 堆栈返回给客户端。
